{
  "name": "hugo-init",
  "version": "1.0.0",
  "scripts": {
    "dev": "npm run clean-public && brunch w -s",
    "prod": "npm run clean-public && brunch b -p",
    "clean-public": "rm -rf public/"
  },
  "devDependencies": {
    "auto-reload-brunch": "^2.7.1",
    "brunch": "^2.10.12",
    "hugo-brunch": "^1.0.1",
    "sass-brunch": "^2.10.4",
    "uglify-js-brunch": "^2.10.0"
  }
}
