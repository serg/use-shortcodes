{
  "name": "hugo-init",
  "version": "1.0.0",
  "scripts": {
    "start": "brunch w -s",
    "build": "npm run clean-public && brunch b -p",
    "preview": "npm run clean-public && brunch b -e development,production",
    "clean-public": "rimraf public/*"
  },
  "devDependencies": {
    "auto-reload-brunch": "^2.7.1",
    "brunch": "^2.10.12",
    "hugo-brunch": "^1.1.0",
    "rimraf": "^2.6.2",
    "sass-brunch": "^2.10.4",
    "uglify-js-brunch": "^2.10.0"
  }
}
